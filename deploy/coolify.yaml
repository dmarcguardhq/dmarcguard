# Coolify Configuration
# https://coolify.io/docs
# Deploy using Docker Compose or Dockerfile

version: "3.8"

services:
  parse-dmarc:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PARSE_DMARC_IMAP_HOST=${IMAP_HOST}
      - PARSE_DMARC_IMAP_PORT=993
      - PARSE_DMARC_IMAP_USERNAME=${IMAP_USERNAME}
      - PARSE_DMARC_IMAP_PASSWORD=${IMAP_PASSWORD}
      - PARSE_DMARC_IMAP_MAILBOX=INBOX
      - PARSE_DMARC_IMAP_USE_TLS=true
      - PARSE_DMARC_DATABASE_PATH=/data/db.sqlite
      - PARSE_DMARC_SERVER_PORT=8080
      - PARSE_DMARC_SERVER_HOST=0.0.0.0
    volumes:
      - parse-dmarc-data:/data
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "-q",
          "--spider",
          "http://localhost:8080/api/statistics",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  parse-dmarc-data:
